version: "3.9"

services:
  dogwalk:
    build: .
    container_name: dog-walk-planner
    ports:
      - "6969:3000"
    volumes:
      - /opt/apps/dog-walk-planner/data:/app/data
      - /opt/apps/dog-walk-planner/logs:/app/logs
      - /opt/apps/dog-walk-planner/config:/app/config
      - /opt/apps/dog-walk-planner/.env:/app/.env:ro
    restart: unless-stopped
    networks:
      - media_network

networks:
  media_network:
    external: true
